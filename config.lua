return {
    core = 'es_extended', -- choose core between 'qb-core' and 'es_extended'

    QBFramework = 'qb-core', -- custom resource exports name
    ESXCoreName = 'ncfw', -- custom resource exports name

    -- OX SETTINGS

    position = 'bottom',
    minDuration = 5000,
    maxDuration = 10000,
    canCancel = false,
    useWhileDead = false,
    disable = true,
    areaSize = 30,
    speedMultiplier = 1,
}
